pipeline {
    agent none

    options {
        timestamps()
        disableConcurrentBuilds()
    }

    stages {
        stage('Agent 1 - Checkout') {
            agent { label 'worker-1' }
            steps {
                cleanWs()
                checkout scm

                sh '''
                    echo "========================================"
                    echo "CHECKOUT STAGE"
                    echo "Running on: $(hostname)"
                    echo "Workspace:  $(pwd)"
                    echo "========================================"
                    git --version
                    java -version
                    mvn -version
                '''
            }
        }

        stage('Agent 1 - Build and Test') {
            agent { label 'worker-1' }
            steps {
                checkout scm

                sh '''
                    echo "========================================"
                    echo "BUILD AND TEST ON WORKER-1"
                    echo "Running on: $(hostname)"
                    echo "========================================"
                    mvn clean test package
                    ls -lh target/
                '''

                junit testResults: 'target/surefire-reports/*.xml',
                      allowEmptyResults: false

                stash name: 'java-application',
                      includes: 'target/*.jar'
            }
        }

        stage('Agent 2 - Receive and Verify') {
            agent { label 'worker-2' }
            steps {
                cleanWs()
                unstash 'java-application'

                sh '''
                    echo "========================================"
                    echo "VERIFY ARTIFACT ON WORKER-2"
                    echo "Running on: $(hostname)"
                    echo "Workspace:  $(pwd)"
                    echo "========================================"
                    java -version
                    ls -lh target/
                    test -s target/jenkins-master-agent-demo-1.0.0.jar
                '''
            }
        }

        stage('Agent 2 - Run Application') {
            agent { label 'worker-2' }
            steps {
                unstash 'java-application'

                sh '''
                    echo "========================================"
                    echo "RUN APPLICATION ON WORKER-2"
                    echo "Running on: $(hostname)"
                    echo "========================================"
                    java -jar target/jenkins-master-agent-demo-1.0.0.jar
                '''

                archiveArtifacts artifacts: 'target/*.jar',
                                 fingerprint: true
            }
        }
    }

    post {
        success {
            echo 'SUCCESS: Worker-1 built and tested the application, and Worker-2 verified and ran it.'
        }
        failure {
            echo 'FAILED: Open Console Output and identify the failed stage.'
        }
        always {
            echo "Pipeline completed with status: ${currentBuild.currentResult}"
        }
    }
}
